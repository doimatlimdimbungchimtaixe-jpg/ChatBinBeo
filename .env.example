# ChatBinBeo needs no environment variables.
# - AI inference runs 100% client-side (browser Web Worker, weights from HuggingFace Hub).
# - History is stored in the browser (localStorage).
# - No login, no server keys. This file exists only so the section is explicit.
