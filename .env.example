# Copy to .env.local (never commit real secrets)
# Required for Google login in production:
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# Random string used to encrypt sessions (required in production):
AUTH_SECRET=
# Optional locally; Vercel sets the host automatically:
# AUTH_URL=http://localhost:3000
